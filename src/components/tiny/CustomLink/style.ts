import styled from "styled-components";

const Style = styled.span`
    display: contents;
    color: inherit;

    > a{
        display: contents;
        font-size: inherit;
        font-weight: inherit;
        color: inherit;
    }
`

export default Style;